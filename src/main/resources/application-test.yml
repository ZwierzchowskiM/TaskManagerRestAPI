spring:
  datasource:
    url: jdbc:h2:mem:test
  jpa:
    defer-datasource-initialization: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.H2Dialect
      show-sql: true

